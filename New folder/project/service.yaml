apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: machine
  labels:
    app: backend
spec:
  selector:
    app: backend
    version: blue
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  